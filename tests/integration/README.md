# Integration Tests

This directory contains cross-module tests, for example:

- collector -> kafka ingestion
- engine consumes kafka and writes clickhouse
- end-to-end alert flow for a sample sigma rule
