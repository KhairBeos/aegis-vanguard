# Rules

This folder is reserved for detection rules and validation fixtures.

Do not add large rule packs until the canonical event schema is stable. Phase 2 should start with a small explainable rule set:

- suspicious shell / encoded command
- authentication brute force
- rare port egress
